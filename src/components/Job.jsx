import React from 'react';

const Job = (props) => {
    // const { companyLogo, jobTitle, location, salary } = props.job;
    return (
        <div className='grid md:grid-cols-2 gap-5'>
            {/* <img className="w-12 py-2 mb-2" src={companyLogo} alt="image" /> */}

        </div>
    );
};

export default Job;